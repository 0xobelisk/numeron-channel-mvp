scrollback: 10000
procs:
  client:
    cwd: apps/web
    shell: pnpm dubhe wait --local-indexer && pnpm dev
  contracts:
    cwd: packages/contracts
    shell: pnpm prod:testnet
  shell:
    cwd: packages/contracts
    shell: pnpm dubhe wait --local-indexer && pnpm dubhe shell
